---
phase: 03-responsive-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hugo/layouts/partials/terminal-frame.html
  - hugo/assets/css/tailwind.css
autonomous: false
requirements: [TERM-03]

must_haves:
  truths:
    - "Terminal frame is visible and readable at 375px width without horizontal overflow"
    - "Status bar hides model text on mobile, keeps green dot + claude-code + 1/4 pagination"
    - "Title bar shows dots left and path right-aligned on mobile"
    - "Terminal height uses svh units on mobile to avoid address bar jank"
    - "Wrapper padding is reduced on mobile to reclaim vertical space"
  artifacts:
    - path: "hugo/layouts/partials/terminal-frame.html"
      provides: "Responsive terminal chrome with Tailwind breakpoint classes"
      contains: "md:col-start-3"
    - path: "hugo/assets/css/tailwind.css"
      provides: "Mobile terminal height override via @media block"
      contains: "--terminal-height: 80svh"
  key_links:
    - from: "hugo/layouts/partials/terminal-frame.html"
      to: "hugo/assets/css/tailwind.css"
      via: "--terminal-height CSS custom property consumed by inline style"
      pattern: "var\\(--terminal-height\\)"
---

<objective>
Make the terminal window chrome responsive so it degrades gracefully on mobile viewports.

Purpose: TERM-03 requires the terminal frame to be usable at 375px (iPhone SE) without horizontal overflow, with simplified chrome on small screens.
Output: Updated terminal-frame.html with Tailwind responsive classes and tailwind.css with mobile --terminal-height override.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-responsive-chrome/03-CONTEXT.md
@.planning/phases/03-responsive-chrome/03-UI-SPEC.md
@.planning/phases/02-terminal-chrome/02-01-SUMMARY.md

<interfaces>
<!-- Current terminal-frame.html structure (Phase 2 output): -->
<!-- base-grid wrapper > terminal-glow card (col-start-3 col-end-11) > title bar + content + status bar -->
<!-- All responsive changes are class additions/replacements on existing elements -->

From hugo/assets/css/tailwind.css (relevant block):
```css
[data-theme="terminal"] {
  --terminal-height: calc(100dvh - 80px - 128px - 96px);
  /* ... other custom properties ... */
}
```

From hugo/layouts/partials/terminal-frame.html:
```html
<!-- Wrapper: class="base-grid" style="padding-top: 96px; padding-bottom: 48px;" -->
<!-- Card: class="terminal-glow col-start-3 col-end-11 flex flex-col overflow-hidden ..." -->
<!-- Title bar: class="flex items-center shrink-0" -->
<!--   Path span: class="flex-1 text-center" -->
<!--   Spacer div: style="width: 56px;" -->
<!-- Status bar model text: <span style="color: var(--color-text-muted);">Opus 4.6 ...</span> -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add responsive classes to terminal-frame.html and mobile height override to tailwind.css</name>
  <files>hugo/layouts/partials/terminal-frame.html, hugo/assets/css/tailwind.css</files>
  <read_first>
    - hugo/layouts/partials/terminal-frame.html (current state - all class changes apply here)
    - hugo/assets/css/tailwind.css (current state - add @media block here)
    - .planning/phases/03-responsive-chrome/03-UI-SPEC.md (exact specs for each responsive change)
    - .planning/phases/03-responsive-chrome/03-CONTEXT.md (locked decisions D-01 through D-09)
  </read_first>
  <action>
Apply all responsive changes per CONTEXT.md decisions and UI-SPEC. All changes use `md:` (768px) as the breakpoint.

**1. Grid columns (D-01):**
On the `.terminal-glow` div (line 3-4 of current file), change:
- `col-start-3 col-end-11` to `col-start-2 col-end-12 md:col-start-3 md:col-end-11`

**2. Wrapper padding (D-09):**
On the `.base-grid` wrapper div (line 1), remove the inline `style="padding-top: 96px; padding-bottom: 48px;"` and replace with Tailwind classes:
- Add classes: `pt-12 pb-6 md:pt-24 md:pb-12`
(Tailwind 4 default: 12 = 48px, 6 = 24px, 24 = 96px, 12 = 48px)

**3. Title bar layout (D-04):**
On the title bar flex container (the div with `class="flex items-center shrink-0"`), change:
- `flex items-center` to `flex items-center justify-between md:justify-start`

On the path `<span>` (currently `class="flex-1 text-center"`), change:
- `flex-1 text-center` to `md:flex-1 md:text-center`

On the spacer `<div>` (currently `style="width: 56px;"`), add:
- `class="hidden md:block"` (keep the existing `style="width: 56px;"`)

**4. Status bar model text (D-02):**
On the model text `<span>` containing "Opus 4.6 &middot; Thinking...", add:
- `class="hidden md:inline"`

**5. Terminal height mobile override (D-07) in tailwind.css:**
Add a new `@media` block AFTER the existing `[data-theme="terminal"]` block (before the `/* TERMINAL CHROME */` comment), structured as:
```css
@media (max-width: 767px) {
  [data-theme="terminal"] {
    --terminal-height: 80svh;
  }
}
```

**Title bar height (D-05):** 40px and padding 0 16px - NO CHANGES needed.
**Status bar height:** 36px and padding 0 16px - NO CHANGES needed.
**min-height: 65vh fallback (D-08):** Already present on the card inline style - NO CHANGES needed.
  </action>
  <verify>
    <automated>cd /Users/janvitu/Code/Projects/website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - terminal-frame.html contains `col-start-2 col-end-12 md:col-start-3 md:col-end-11`
    - terminal-frame.html contains `pt-12 pb-6 md:pt-24 md:pb-12` on the base-grid div
    - terminal-frame.html does NOT contain `style="padding-top: 96px; padding-bottom: 48px;"`
    - terminal-frame.html contains `justify-between md:justify-start` on the title bar div
    - terminal-frame.html contains `md:flex-1 md:text-center` on the path span
    - terminal-frame.html contains `class="hidden md:block"` on the spacer div
    - terminal-frame.html contains `class="hidden md:inline"` on the model text span
    - tailwind.css contains `@media (max-width: 767px)` block with `--terminal-height: 80svh;`
    - `npm run build` exits 0 with no errors
  </acceptance_criteria>
  <done>All responsive Tailwind classes applied to terminal-frame.html and mobile --terminal-height override added to tailwind.css. Hugo build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification at mobile and desktop widths</name>
  <files>hugo/layouts/partials/terminal-frame.html, hugo/assets/css/tailwind.css</files>
  <action>
Present the dev server to the user for visual verification at mobile (375px) and desktop (768px+) viewports. Start `npm run dev` if not already running.
  </action>
  <what-built>Responsive terminal chrome: mobile-first grid (col 2-12), reduced wrapper padding (48px/24px), title bar with justify-between layout (dots left, path right), hidden model text in status bar, and 80svh terminal height on mobile.</what-built>
  <how-to-verify>
    1. Run `npm run dev` (if not already running) and open http://localhost:1313/how-i-ai
    2. Open browser DevTools and toggle device toolbar (Cmd+Shift+M in Chrome)
    3. Set viewport to 375px width (iPhone SE):
       - Terminal frame should be visible with no horizontal scrollbar
       - Title bar: traffic light dots on the left, ~/how-i-ai right-aligned
       - Status bar: green dot + "claude-code" on left, "1/4" in orange on right, NO "Opus 4.6" text
       - Terminal height should fill most of the viewport (~80%) with header/footer still visible
       - Wrapper padding should be visibly smaller than desktop
    4. Set viewport to 768px+ (or full desktop width):
       - Grid should snap to narrower col 3-11 layout
       - Title bar: path centered with spacer visible
       - Status bar: "Opus 4.6 - Thinking..." visible on right side
       - Wrapper padding should be full (96px top, 48px bottom)
    5. Resize slowly from 375px to 900px - no layout jumps or broken states at the 768px breakpoint
  </how-to-verify>
  <verify>
    <automated>cd /Users/janvitu/Code/Projects/website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - User confirms no horizontal overflow at 375px
    - User confirms title bar layout change at md: breakpoint
    - User confirms status bar model text hidden on mobile
    - User types "approved"
  </acceptance_criteria>
  <done>User visually approved responsive terminal chrome at mobile and desktop viewports.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Hugo build completes without errors
- No horizontal overflow at 375px viewport width
- Status bar model text hidden on mobile, visible on desktop
- Title bar layout switches at md: breakpoint
- Terminal height uses svh on mobile
- All existing Phase 2 visual elements preserved (dots, glow, status bar content)
</verification>

<success_criteria>
- Terminal frame renders correctly at 375px width with no horizontal overflow
- Chrome simplifies on mobile (model text hidden, title bar right-aligned)
- Terminal height uses 80svh on mobile, existing calc on desktop
- Layout transitions cleanly at 768px breakpoint
- Human visual verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-responsive-chrome/03-01-SUMMARY.md`
</output>
