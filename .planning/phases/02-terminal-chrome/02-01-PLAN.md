---
phase: 02-terminal-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hugo/assets/css/tailwind.css
  - hugo/layouts/partials/terminal-frame.html
  - hugo/layouts/how-i-ai/list.html
  - hugo/content/how-i-ai/_index.en.html
autonomous: false
requirements: [TERM-01, TERM-02, TERM-04]

must_haves:
  truths:
    - "A title bar is visible at the top of the terminal frame with red/yellow/green dots and monospace path ~/how-i-ai"
    - "A status bar is visible at the bottom showing green dot + claude-code, 1/4 in accent orange, and Opus 4.6 + Thinking..."
    - "A subtle warm glow is visible behind the terminal card on the dark background"
    - "The terminal card fills the viewport between header and footer with content scrolling inside it"
  artifacts:
    - path: "hugo/layouts/partials/terminal-frame.html"
      provides: "Terminal card wrapper with title bar, content area, status bar, and glow"
      min_lines: 40
    - path: "hugo/assets/css/tailwind.css"
      provides: "Terminal chrome CSS styles for glow, monospace labels, and status bar layout"
      contains: ".terminal-glow"
    - path: "hugo/layouts/how-i-ai/list.html"
      provides: "Wires terminal-frame partial into the page"
      contains: "terminal-frame"
    - path: "hugo/content/how-i-ai/_index.en.html"
      provides: "Cleaned content that sits inside terminal frame content area"
  key_links:
    - from: "hugo/layouts/how-i-ai/list.html"
      to: "hugo/layouts/partials/terminal-frame.html"
      via: "Hugo partial call"
      pattern: 'partial "terminal-frame.html"'
    - from: "hugo/layouts/partials/terminal-frame.html"
      to: "hugo/assets/css/tailwind.css"
      via: "CSS classes for glow and chrome styling"
      pattern: "terminal-glow"
---

<objective>
Build the terminal window chrome that visually frames the /how-i-ai page: a title bar with macOS-style colored dots and monospace path, a status bar with Claude model info and green indicator, and a subtle background glow behind the card.

Purpose: Transform the dark-themed page into a recognizable terminal window, delivering the core visual identity of the "How I AI" experience.
Output: A Hugo partial (`terminal-frame.html`) wired into the page layout, plus supporting CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-terminal-chrome/02-CONTEXT.md
@.planning/phases/02-terminal-chrome/02-UI-SPEC.md
@.planning/phases/01-dark-theme-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Existing CSS custom properties from Phase 1 (tailwind.css lines 72-81) -->
[data-theme="terminal"] {
  --color-bg: #1a1a1a;
  --color-bg-surface: #252525;
  --color-border: #3a3a3a;
  --color-text: #e0e0e0;
  --color-text-muted: #888888;
  --color-accent: #ff4309;
}

<!-- Existing grid utility (tailwind.css line 15) -->
.base-grid {
  @apply grid grid-cols-12;
}

<!-- Current list.html (entire file) -->
{{ define "main" }}
  {{ .Content }}
{{ end }}

<!-- Current baseof.html body structure -->
<body class="overflow-x-hidden" data-theme="terminal">
  <!-- skip link, grid, header, contactSide -->
  <main id="main">
    {{ block "main" . }}{{ .Content }}{{ end }}
  </main>
  <!-- footer, JS -->
</body>

<!-- Header is fixed positioned: class="fixed inset-x-0 top-[20px] z-[1000]" -->
<!-- Footer has py-16 padding -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add terminal chrome CSS and create terminal-frame partial</name>
  <files>hugo/assets/css/tailwind.css, hugo/layouts/partials/terminal-frame.html</files>
  <read_first>
    - hugo/assets/css/tailwind.css (current state, see where to append new styles)
    - hugo/layouts/how-i-ai/baseof.html (understand body structure, header/footer presence)
    - hugo/layouts/partials/header.html (fixed position, top-[20px], to understand height ~64px)
    - hugo/layouts/partials/footer.html (py-16, to understand height ~128px)
    - .planning/phases/02-terminal-chrome/02-UI-SPEC.md (authoritative visual spec)
    - .planning/phases/02-terminal-chrome/02-CONTEXT.md (locked decisions D-01 through D-12)
  </read_first>
  <action>
**CSS additions to `hugo/assets/css/tailwind.css`:**

Append the following INSIDE the existing `[data-theme="terminal"]` block (after the `--color-accent` line, before the closing brace), or as new rules scoped under `[data-theme="terminal"]`:

1. Terminal glow pseudo-element class:
```css
[data-theme="terminal"] .terminal-glow {
  position: relative;
}
[data-theme="terminal"] .terminal-glow::before {
  content: "";
  position: absolute;
  inset: -10%;
  z-index: -1;
  background: radial-gradient(ellipse 80% 60% at 50% 50%, rgba(255, 67, 9, 0.06) 0%, transparent 70%);
  pointer-events: none;
}
```

2. Terminal card height variable (add inside the `[data-theme="terminal"]` custom properties block):
```css
--terminal-height: calc(100dvh - 80px - 128px - 96px);
```
Where: 80px = header (fixed, ~64px + 20px offset), 128px = footer (py-16 = 64px * 2), 96px = breathing room (48px top + 48px bottom). Per UI-SPEC, minimum 400px.

**Hugo partial `hugo/layouts/partials/terminal-frame.html`:**

Create this file with the complete terminal chrome markup. The partial receives the full page context (`.`) and renders `.Content` inside the scrollable content area.

Structure (per D-08, D-09, D-10, D-11 and UI-SPEC Layout Contract):

```html
<div class="base-grid" style="padding-top: 48px; padding-bottom: 48px;">
  <div class="terminal-glow col-start-3 col-end-11 flex flex-col overflow-hidden rounded-lg border border-solid"
       style="border-color: var(--color-border); background-color: var(--color-bg-surface); height: var(--terminal-height); min-height: 400px; border-radius: 8px;"
       role="region" aria-label="Terminal window">

    {{/* Title bar - per D-01, D-02 */}}
    <div class="flex items-center shrink-0" style="height: 40px; padding: 0 16px; border-bottom: 1px solid var(--color-border);" aria-hidden="true">
      <div class="flex items-center" style="gap: 8px;">
        <span style="width: 12px; height: 12px; border-radius: 50%; background-color: #ff5f56; display: inline-block;"></span>
        <span style="width: 12px; height: 12px; border-radius: 50%; background-color: #ffbd2e; display: inline-block;"></span>
        <span style="width: 12px; height: 12px; border-radius: 50%; background-color: #27c93f; display: inline-block;"></span>
      </div>
      <span class="flex-1 text-center" style="font-family: ui-monospace, 'SF Mono', Menlo, monospace; font-size: 14px; line-height: 1.2; color: var(--color-text);">~/how-i-ai</span>
      {{/* Invisible spacer to center the title text, same width as dots */}}
      <div style="width: 56px;"></div>
    </div>

    {{/* Content area - scrollable, per D-10 */}}
    <div class="flex-1 overflow-y-auto" style="padding: 32px 24px;">
      {{ .Content }}
    </div>

    {{/* Status bar - pinned bottom per D-11, layout per D-03 through D-07 */}}
    <div class="flex items-center justify-between shrink-0" style="height: 36px; padding: 0 16px; border-top: 1px solid var(--color-border);" aria-hidden="true">
      {{/* Left: green dot + claude-code per D-03 */}}
      <div class="flex items-center" style="gap: 8px; font-family: ui-monospace, 'SF Mono', Menlo, monospace; font-size: 14px; line-height: 1.2; color: var(--color-text);">
        <span style="width: 8px; height: 8px; border-radius: 50%; background-color: #27c93f; display: inline-block;"></span>
        <span>claude-code</span>
      </div>
      {{/* Center: pagination counter per D-04, D-07 - accent orange with glow */}}
      <div style="font-family: ui-monospace, 'SF Mono', Menlo, monospace; font-size: 14px; line-height: 1.2; color: var(--color-accent); text-shadow: 0 0 8px rgba(255, 67, 9, 0.6);">
        1/4
      </div>
      {{/* Right: model info per D-05 */}}
      <div style="font-family: ui-monospace, 'SF Mono', Menlo, monospace; font-size: 14px; line-height: 1.2; color: var(--color-text-muted);">
        Opus 4.6 &middot; Thinking...
      </div>
    </div>

  </div>
</div>
```

Key details:
- The `&middot;` between "Opus 4.6" and "Thinking..." renders as Unicode middle dot U+00B7 (not a period)
- Traffic light dots: 12px diameter, 8px gap, colors #ff5f56 / #ffbd2e / #27c93f (per D-02)
- Green status indicator: 8px diameter, #27c93f (per D-03)
- Pagination `1/4` text: color `var(--color-accent)` with `text-shadow: 0 0 8px rgba(255, 67, 9, 0.6)` for neon orange glow (per D-07)
- Card uses `col-start-3 col-end-11` on the 12-column grid (per D-09)
- Title bar `aria-hidden="true"`, status bar `aria-hidden="true"` (per UI-SPEC Accessibility Contract)
- Outer card has `role="region" aria-label="Terminal window"` (per UI-SPEC Accessibility Contract)
- The invisible spacer div (56px = 3 dots x 12px + 2 gaps x 8px + 4px rounding) keeps the title text centered
  </action>
  <verify>
    <automated>grep -c "terminal-glow" hugo/assets/css/tailwind.css && grep -c "terminal-glow" hugo/layouts/partials/terminal-frame.html && grep -c "~/how-i-ai" hugo/layouts/partials/terminal-frame.html && grep -c "claude-code" hugo/layouts/partials/terminal-frame.html && grep -c "#ff5f56" hugo/layouts/partials/terminal-frame.html && grep -c "1/4" hugo/layouts/partials/terminal-frame.html && grep -c "Opus 4.6" hugo/layouts/partials/terminal-frame.html</automated>
  </verify>
  <acceptance_criteria>
    - hugo/assets/css/tailwind.css contains `.terminal-glow` class with `radial-gradient(ellipse 80% 60%` and `rgba(255, 67, 9, 0.06)`
    - hugo/assets/css/tailwind.css contains `--terminal-height: calc(100dvh`
    - hugo/layouts/partials/terminal-frame.html exists and contains `role="region"` and `aria-label="Terminal window"`
    - terminal-frame.html contains `~/how-i-ai` (title bar path, per D-01)
    - terminal-frame.html contains three traffic light dot spans with colors `#ff5f56`, `#ffbd2e`, `#27c93f` (per D-02)
    - terminal-frame.html contains `claude-code` text with green indicator dot `#27c93f` (per D-03)
    - terminal-frame.html contains `1/4` with `color: var(--color-accent)` and `text-shadow` (per D-04, D-07)
    - terminal-frame.html contains `Opus 4.6` and `Thinking...` with `&middot;` entity (per D-05, D-06)
    - terminal-frame.html contains `{{ .Content }}` inside an `overflow-y-auto` div (per D-10)
    - terminal-frame.html contains `aria-hidden="true"` on title bar and status bar divs (per UI-SPEC accessibility)
    - terminal-frame.html contains `col-start-3 col-end-11` (per D-09)
    - terminal-frame.html contains `min-height: 400px` (per UI-SPEC)
  </acceptance_criteria>
  <done>Terminal frame partial exists with all chrome elements (title bar with dots + path, status bar with indicator/pagination/model info, glow pseudo-element) and supporting CSS is in tailwind.css</done>
</task>

<task type="auto">
  <name>Task 2: Wire terminal-frame partial into list.html and update content file</name>
  <files>hugo/layouts/how-i-ai/list.html, hugo/content/how-i-ai/_index.en.html</files>
  <read_first>
    - hugo/layouts/how-i-ai/list.html (current: just {{ .Content }} in main block)
    - hugo/content/how-i-ai/_index.en.html (current placeholder with base-grid wrapper)
    - hugo/layouts/partials/terminal-frame.html (just created in Task 1 - need to verify it uses {{ .Content }})
    - hugo/layouts/how-i-ai/baseof.html (main block definition)
  </read_first>
  <action>
**Update `hugo/layouts/how-i-ai/list.html`:**

Replace the current content:
```
{{ define "main" }}
  {{ .Content }}
{{ end }}
```

With:
```
{{ define "main" }}
  {{ partial "terminal-frame.html" . }}
{{ end }}
```

The terminal-frame partial handles the base-grid, column placement, and renders `{{ .Content }}` inside the scrollable content area. The list.html just calls the partial with the page context.

**Update `hugo/content/how-i-ai/_index.en.html`:**

Remove the `base-grid` wrapper div from the content file since the terminal-frame partial now provides the grid and column placement. The content file should contain only what goes INSIDE the terminal content area.

Replace current content (after frontmatter):
```html
<div class="base-grid">
  <div class="col-start-2 col-end-12 py-16">
    <h1 class="text-4xl font-semibold" style="font-size: 2.25rem;">How I AI</h1>
    <p class="mt-4" style="color: var(--color-text-muted, #888888);">Terminal interface loading -- content coming in Phase 4.</p>
  </div>
</div>
```

With:
```html
<h1 class="text-4xl font-semibold" style="font-size: 2.25rem; color: var(--color-text);">How I AI</h1>
<p class="mt-4" style="color: var(--color-text-muted);">Terminal interface loading -- content coming in Phase 4.</p>
```

The grid wrapper and padding are removed because the terminal frame's content area already provides padding (32px top, 24px sides). The h1 gets explicit `color: var(--color-text)` to ensure it renders in the light text color on dark background.

**Build verification:**

After both files are updated, run `npm run build` from the project root to confirm Hugo builds successfully with the new partial.
  </action>
  <verify>
    <automated>cd /Users/janvitu/Code/Projects/website && grep -c 'terminal-frame' hugo/layouts/how-i-ai/list.html && grep -vc 'base-grid' hugo/content/how-i-ai/_index.en.html && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - hugo/layouts/how-i-ai/list.html contains `partial "terminal-frame.html" .`
    - hugo/layouts/how-i-ai/list.html does NOT contain bare `{{ .Content }}` outside the partial call
    - hugo/content/how-i-ai/_index.en.html does NOT contain `base-grid` class (grid is now in the partial)
    - hugo/content/how-i-ai/_index.en.html contains `How I AI` heading and placeholder text
    - `npm run build` exits with code 0 (Hugo successfully resolves the partial)
    - dist/how-i-ai/index.html contains `terminal-glow` class (glow renders in output)
    - dist/how-i-ai/index.html contains `claude-code` (status bar renders in output)
    - dist/how-i-ai/index.html contains `~/how-i-ai` (title bar renders in output)
  </acceptance_criteria>
  <done>Terminal frame is wired into the page, content file is cleaned up, Hugo builds successfully, and dist output contains the full terminal chrome markup</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of terminal chrome</name>
  <files>none</files>
  <action>
Present the completed terminal chrome for human visual verification. The dev server should already be running from Task 2's build step. If not, start it with `npm run dev`.

**What was built:**
Terminal window chrome framing the /how-i-ai page: title bar with red/yellow/green dots and ~/how-i-ai path, status bar with green indicator + claude-code, orange-glowing 1/4 pagination, and Opus 4.6 model info, plus a subtle warm glow behind the card.

**How to verify:**
1. Open http://localhost:1313/how-i-ai/ in a browser
2. Verify the terminal card is visible with:
   - Title bar at top: three colored dots (red, yellow, green) on the left, "~/how-i-ai" centered in monospace
   - Content area in the middle showing "How I AI" heading and placeholder text
   - Status bar at bottom: green dot + "claude-code" on left, "1/4" in orange glow in center, "Opus 4.6 . Thinking..." on right
3. Verify the card has a subtle warm orange glow behind it (visible against the #1a1a1a background)
4. Verify the card fills the viewport height between header and footer
5. Scroll to verify: if content overflows, it scrolls within the card (status bar stays pinned)
6. Navigate to any other page (e.g., /about/) to confirm no visual changes outside /how-i-ai
  </action>
  <verify>Human approves visual output</verify>
  <done>User confirms terminal chrome renders correctly: title bar with dots and path, status bar with all three sections, background glow visible, card fills viewport</done>
</task>

</tasks>

<verification>
- `npm run build` completes with exit code 0
- `dist/how-i-ai/index.html` contains terminal chrome markup (terminal-glow, ~/how-i-ai, claude-code, 1/4, Opus 4.6)
- No other pages in dist/ reference terminal-glow or terminal-frame classes
- Visual inspection confirms title bar, status bar, and glow render correctly
</verification>

<success_criteria>
- TERM-01: Title bar visible with red/yellow/green dots and ~/how-i-ai monospace path
- TERM-02: Status bar visible with green indicator + claude-code, 1/4 pagination in accent orange, Opus 4.6 model info
- TERM-04: Subtle background glow visible behind the terminal card
- Terminal card fills viewport height between header and footer, content scrolls within
- Hugo build succeeds, no regressions on other pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-terminal-chrome/02-01-SUMMARY.md`
</output>
